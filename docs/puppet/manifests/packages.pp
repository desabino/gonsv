# ------------- Any Packages
class packages {
    #package { ['vim-enhanced', 'nc', 'zip', 'unzip', 'mlocate', 'php']:
    #    ensure => 'installed'
    #}
    
    #package { ['php']:
    #    ensure => 'installed'
    #}
}